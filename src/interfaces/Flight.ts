export default interface Flight {
  departureTime: string
  arrivalTime: string
  departureLocation: string
  arrivalLocation: string
  stops: string[]
  plane: string
  bookedSeats: string[]
  reviews: string[]
}
